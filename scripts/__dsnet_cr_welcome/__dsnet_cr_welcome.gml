///@param buffer
var buffer = argument0;
connected = true;
if __obj_dsnet_container.debug debug_log("DSNET: Server welcomed client - connection stable.");

with (parent) {
	script_execute(other.func_connect);
}